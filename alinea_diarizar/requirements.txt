# ══════════════════════════════════════════════════════════════════════════
# transcribe_diarize.py — dependencias
# ══════════════════════════════════════════════════════════════════════════
#
# Instalación:
#   pip install -r requirements.txt
#
# Nota GPU NVIDIA (CUDA) — instala PyTorch con CUDA *antes* de este fichero:
#   CUDA 12.x :  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11.8 :  pip install torch --index-url https://download.pytorch.org/whl/cu118
#   Luego:       pip install -r requirements.txt
#
# Diarización — acepta las licencias en HuggingFace y crea un token:
#   https://huggingface.co/pyannote/speaker-diarization-3.1
#   https://huggingface.co/pyannote/segmentation-3.0
#   https://huggingface.co/settings/tokens
#   export HF_TOKEN=hf_tu_token
#
# ══════════════════════════════════════════════════════════════════════════

# Backend para Mac Apple Silicon (Neural Engine, ~10× real-time)
mlx-whisper

# Transcripción + alineación a nivel de palabra + diarización integrada
# (incluye faster-whisper, pyannote.audio, transformers, torch, etc.)
whisperx

# TextGrid de Praat
praatio>=6.0.0

# Formato ELAN (.eaf)
pympi-ling>=1.70
